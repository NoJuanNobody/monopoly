// This is the application entry point

export class App {

  version: string;

  constructor() {
    console.log('Awesome App is running!');
  }

}


/**
 * 
 * Monopoly
 * 
 * player wallet
 * bank
 * money.denominations
 */

 /**
  * the gameboard class keeps track of and iterates the state of the board,
  * the available peices and passively notifies the players of event stacks
  * that are stored on a given game tile
  */
 
 
/**
 * Game tiles are placed in a list on a game board.
 *  when a player lands on this game tile, they will be prompted
 * iterate through the GameEvents that are present in the Event Stack
 */







enum denominations {
  ONE = 1,
  FIVE = 5,
  TEN = 10,
  TWENTY = 20,
  FIFTY = 50,
  HUNDRED = 100,
  FIVE_HUNDRED
}

